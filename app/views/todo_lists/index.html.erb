<h1>My Todo lists</h1>


<% @todo_lists.each do |list| %>
  <p><%= link_to list.title, todo_list_path(list.id) %>
  -
  <%= link_to "Delete", todo_list_path(list), method: :delete, data: { confirm: "Are you sure ?" } %></p>
<% end %>


<%= link_to "Add a list", new_todo_list_path %>
